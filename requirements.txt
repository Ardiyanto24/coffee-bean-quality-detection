# Coffee Bean Quality Detection -- Python dependencies
#
# These notebooks were developed and run on Colab/Kaggle-style hosted GPU
# runtimes (each notebook installs its own extra packages via `!pip install -q ...`
# in its first cell). This file consolidates everything needed to run the full
# pipeline locally or on your own GPU environment. No exact version lockfile
# was captured during the original runs, so versions below are floors that are
# known compatible with the codebase, not an exact reproduction of the
# original runtime -- see docs/reproducing-training.md for details and caveats.
#
# Python >= 3.10 recommended.

# --- Deep learning ---
torch>=2.1
torchvision>=0.16
timm>=0.9            # ConvNeXt-Tiny, DeiT-Tiny backbones

# --- Classical ML / tabular ---
scikit-learn>=1.3
lightgbm>=4.0

# --- Hyperparameter optimization ---
optuna>=3.4

# --- Explainability (XAI) ---
captum>=0.7           # Grad-CAM, Integrated Gradients, Attention Rollout, TCAV building blocks
shap>=0.44             # SHAP values for the tabular gradient-boosting model

# --- Data handling / image processing ---
numpy>=1.26
pandas>=2.1
opencv-python>=4.8
Pillow>=10.0
scipy>=1.11

# --- Visualization ---
matplotlib>=3.8
seaborn>=0.13
tqdm>=4.66

# --- Data & model versioning (DVC + Cloudflare R2 / S3-compatible remote) ---
dvc[s3]>=3.30

# --- Notebook execution ---
jupyter>=1.0

# --- Optional: only needed for `CBQD - EDA v2 (FiftyOne).ipynb` ---
# fiftyone>=0.23
